import { Effect, run } from 'effect-js';

const main = Effect.gen(function* () {
  // Initialize any necessary configurations or middleware here

  // Example: Set up logging or other middleware
  console.log("Initializing markdown-it-image-size-effect...");

  // Further setup can be done here
});

// Run the Effect.js runtime
run(main);